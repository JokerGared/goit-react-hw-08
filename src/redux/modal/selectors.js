export const selectModalIsOpen = (state) => state.modal.isOpen;
export const selectModalId = (state) => state.modal.contactId;
export const selectModalIsDeleting = (state) => state.modal.isDeleting;
export const selectContactInfo = (state) => state.modal.contactInfo;
